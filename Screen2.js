import React from 'react';
import ReactDOM from 'react-dom';

const Screen2 = params => (
  <p>This is a simple screen for testing continuous compilation</p>
);

ReactDOM.render(<Screen2 />, document.getElementById('root'));
